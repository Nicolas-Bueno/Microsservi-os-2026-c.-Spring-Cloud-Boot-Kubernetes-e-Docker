server:
  port: 8888
spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Nicolas-Bueno/nb-config-server
          search-paths: 'greeting-server*'
          

